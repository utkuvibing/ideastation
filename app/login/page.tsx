import { login } from '../actions';

export default async function Login({ searchParams }: { searchParams: Promise<{ error?: string }> }){const params = await searchParams; return <main className="min-h-screen grid place-items-center p-6"><form action={login} className="card w-full max-w-sm space-y-4"><div><p className="text-sm opacity-60">AsunaTech</p><h1 className="text-2xl font-bold">IdeaStation Login</h1></div>{params.error && <div className="rounded-lg border border-red-500/40 bg-red-500/10 px-3 py-2 text-sm text-red-600 dark:text-red-300">Email veya password hatalı.</div>}<input name="email" type="email" placeholder="Email" required className="w-full"/><input name="password" type="password" placeholder="Password" required className="w-full"/><button className="w-full">Giriş Yap</button></form></main>}
